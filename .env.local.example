# Vercel KV (Redis) — auto-injected when you create a KV store in Vercel Dashboard
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Cron endpoint auth — generate a random string: openssl rand -hex 32
CRON_SECRET=
